--RL
--2\21\2024
--CODE EXECUTION ORDER\порядок выполнения кода. Сори за инглиш

SELECT --Как-то так строится запрос чаще всего
FROM
WHERE
GROUP BY
ORDER BY
HAVING

FROM --так работает код. Определяет имена таблиц
WHERE --Выполняет фильтрацию
GROUP BY --Образует группы строк
HAVING --фильтрует вновь
SELECT --только теперь определяет столбцы для вывода
ORDER BY --наводит порядки в результате работы запроса